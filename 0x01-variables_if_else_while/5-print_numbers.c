#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10
 * Start from 0
 * Return: 0
 */

int main(void)
{
	int i;

	while (i < 10)
	{
		printf("%d", i);
		i++;
		return (0);
	}
}
